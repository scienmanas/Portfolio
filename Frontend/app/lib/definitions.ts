export type BlogPostMetaDataProps = {
  cardData: {
    title: string;
    image: string;
    publishedDate: string;
    description: string;
    tags: string[];
    slug: string;
  };
};


export type submissionLoaderProps = {
  width: number;
  height: number;
  color: string;
};